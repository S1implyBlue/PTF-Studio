# PTF Studio Beta 0.9.4.2

## Bundled sample-theme update

- Replaced the previous bundled sample with `PTF_STUDIO_SAMPLE_THEME.ptf`.
- Removed `S1mplyBlue.ptf` from the release package.
- Rebuilt the embedded browser sample from the new PTF so **Load sample** opens the same theme on macOS and Windows.
- Updated regression tests to verify the new sample's Title, Product ID, colour value, and embedded payload.
- Updated cache identifiers, documentation, macOS resources, and checksums.

No importer, exporter, resizing, or compression behaviour was changed in this maintenance build.
